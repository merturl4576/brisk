bin/
obj/
*.user
.vs/
.superpowers/

# Working material that lives beside the project but is not part of it:
# reference recordings, screenshots and notes. Kept on disk, out of history —
# a 12 MB video is effectively permanent once it lands in a commit.
*.mp4
image*.png
briskhakkinda.txt

# Built by scripts/publish.ps1 — reproducible from any commit, so never committed.
artifacts/

# Generated agent tooling, not part of the project.
.claude/

# Workbench state files record the machine's true pre-plant state — local only.
tools/workbench/.state/

# Design snapshots — evidence for human eyes, never committed
.snapshots/
